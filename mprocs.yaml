procs:
  nvim:
    cmd: [nvim, .]
  test:
    cmd: [make, test]
  repl:
    cmd: [make]
    autostart: false
  clean:
    cmd: [make, clean]
    autostart: false
proc_list_width: 17
hide_keymap_window: true
