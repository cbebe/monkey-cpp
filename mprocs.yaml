procs:
  nvim:
    cmd: [nvim, .]
  test:
    cmd: [make, -j, test]
  repl:
    cmd: [make, -j, run]
    autostart: false
  clean:
    cmd: [make, clean]
    autostart: false
  test_leak:
    cmd: [make, -j, test-leak]
    autostart: false
proc_list_width: 17
hide_keymap_window: true
